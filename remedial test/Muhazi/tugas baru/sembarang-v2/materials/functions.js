function walk (kjbsdv,sdv,sdv,sdv,sdsd,vsdv) {
    // start point -- code here
    console.log('jalan kaki ke stasiun')
    // end point -- until here
}

function openTheDoor () {
    console.log('cabut dr kosan')
}

function run () {
    console.log('telat, jadi lari')
}

function arrived () {
    console.log('sampai di kantor')
}

// function getUserDetail () {
//     Model.findOne({where: {id: user_id}})
//     // walk()
//     // arrived()
//     // openTheDoor()
// }

const rectangleArea = (p = 1, l = 1) => p * l // also works. only applied for one liner

const getUserDetail = userID => {
    
}


// for 
// while
// if else switch
// % + - = * /

// walk is a variable, that is a function. an anonymous function
// arrow function is just a fancy way to declare a function. ES6 only